apiVersion: v1
kind: Service
metadata:
  name: dashboard-svc
  namespace: kubernetes-dashboard
  labels:
    k8s-app: kubernetes-dashboard
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8443
      nodePort: 30001
  selector:
    k8s-app: kubernetes-dashboard